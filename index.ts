// Package entry for AssemblyScript module resolution (`import { dtoa } from "zmij-as"`).
// Re-exports the public surface only — mirrors assembly/index.ts; keep in sync.
export { dtoa, dtoa_buffered, ftoa, ftoa_buffered } from "./assembly/dtoa";
